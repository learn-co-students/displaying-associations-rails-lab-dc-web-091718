<h1>Artists</h1>

<ul>
  <% @artists.each do |a| %>
  <li><%= link_to a.name, artist_path(a) %></li>
  <li><%= pluralize(a.song_count, 'song')%></li>
<!-- shows the song count for each artist (#song_count in Artist model) -->
  <%end%>
</ul>
